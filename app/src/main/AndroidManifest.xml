<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aarshil.onactivityforresult">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerViewActivity"
            android:theme="@style/AppTheme">
        </activity>

                <activity android:name=".SplashActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ResultActivity" />
        <activity android:name=".ImagePickActivity" />
        <activity android:name=".RatingbarSeekbar" />
        <activity android:name=".WebviewActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DatePickerTimePickerActivity" />
        <activity android:name=".VideoViewActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".CustomToast_dialogActivity" />
        <activity android:name=".ListViewWithBaseAdapterActivity" />
        <activity android:name=".GridViewActivity" />
        <activity android:name=".SharedPrefActivity" />
        <activity
            android:name=".Main3Activity"
            android:label=".Main3Activity"
            android:theme="@style/AppTheme" />
        <activity android:name=".TabActivity" />

        <service android:name=".MyService" />

        <receiver android:name=".Broadcast" />

        <activity android:name=".ServiceActivity"/>
    </application>

</manifest>